defaults:
  - ../base/data.yaml
  - ../base/preprocessing.yaml
  - ../base/model.yaml
  - ../base/training.yaml
  - ../base/evaluation.yaml
  - ../base/mlflow.yaml
experiment:
  name: "univariate_nlinear"
  type: "univariate_per_series"
  model: "nlinear"
  hyperparams:
    input_chunk_length: 24
    output_chunk_length: 6
    n_epochs: 2
    batch_size: 8
