defaults:
  - ../base/preprocessing.yaml
  - ../base/model.yaml
  - ../base/training.yaml
  - ../base/evaluation.yaml
  - ../base/mlflow.yaml
  - ../base/tuning.yaml

data:
  path: "mlproject/data/ETTh1.csv"   # optional demo
  # train_path: "mlproject/data/ETTh1.csv"   # optional demo
  # test_path: "mlproject/data/ETTh1.csv"   # optional demo
  # val_path: "mlproject/data/ETTh1.csv"   # optional demo
  index_col: "date"
  target_columns: ["HUFL","MUFL",]
  features: ["HUFL", "MUFL", "mobility_inflow"]
  return_type: pandas
  type: timeseries


experiment:
  name: "xgboost"
  type: "timeseries"
  model: "xgboost"
  model_type: "ml"
  hyperparams:
    input_chunk_length: 24
    output_chunk_length: 6
    n_targets: 2
    type: "regression"
    args:
      n_estimators: 20
      max_depth: 3
      learning_rate: 0.1
      objective: "reg:squarederror"
      early_stopping_round: 15
